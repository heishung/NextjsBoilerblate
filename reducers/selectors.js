export const authSelector = (state) => state.auth;
export const gameSelector = (state) => state.game;
export const guildSelector = (state) => state.guild;
export const giftSelector = (state) => state.gift;
